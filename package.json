{
  "name": "dc-fast",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "cs": "npx cspell lint '**' --no-progress --words-only"
  }
}
